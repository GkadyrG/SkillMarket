package com.example.dotalink.feature.party.repository;

import com.example.dotalink.feature.party.model.PartyApplication;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PartyApplicationRepository extends JpaRepository<PartyApplication, Long> {
}
